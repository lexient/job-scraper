[project]
name = "job-scraper"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.13",
    "beautifulsoup4>=4.12",
    "curl-cffi>=0.7",
    "lxml>=4.9",
    "markdownify>=0.11",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0",
    "sqlmodel>=0.0.22",
]
